import type { Config } from 'tailwindcss';

const config: Config = {
  // Tailwind v4 uses @source in CSS for content scanning
  // Theme is configured in global.css using @theme
  darkMode: 'class',
};

export default config;
