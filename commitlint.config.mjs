// commitlint.config.mjs
export default {
    extends: ['@commitlint/config-conventional'],
    rules: {
      // Optional: Override rules here.
      // Example: Enforce that the scope must be one of your project names
      // 'scope-enum': [2, 'always', ['web', 'api', 'mobile', 'shared-ui']],
    },
  };